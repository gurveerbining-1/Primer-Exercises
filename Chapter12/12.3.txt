Does this class need const versions of push_back and pop_back? If
so, add them. If not, why aren’t they needed?

No because these operations modify StrBlob, because the point of these functions is to change the object const versions would not make sense.