Explain what if anything is wrong with the following function.
bool b() {
int* p = new int;
// ...
return p;
}

p is a pointer to int that is declared using the new keyword and returned at the end of the function, the expected return type
is bool for b() so this gives an error (compilation) (it could potentially compile though on some compilers), another issue
is that the pointer is uninitialized.